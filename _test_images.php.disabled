<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Test Images</title>
    <style>
        body { background: #1a1a1a; color: white; padding: 40px; }
        img { max-width: 800px; border: 3px solid lime; margin: 20px 0; display: block; }
        h2 { color: #3b82f6; }
    </style>
</head>
<body>
    <h1>🔍 Test des Images</h1>

    <?php
    $images_json = file_get_contents('data.json');
    $images = json_decode($images_json, true);

    foreach ($images as $image) {
        echo "<h2>{$image['year']} - {$image['description']}</h2>";
        echo "<p>Chemin : <code>{$image['url']}</code></p>";

        // Vérifier si le fichier existe
        if (file_exists($image['url'])) {
            echo "<p style='color: lime;'>✓ Fichier existe sur le serveur</p>";
        } else {
            echo "<p style='color: red;'>✗ Fichier INTROUVABLE sur le serveur</p>";
        }

        echo "<img src='{$image['url']}' alt='{$image['description']}'>";
        echo "<hr style='border-color: #333; margin: 40px 0;'>";
    }
    ?>

</body>
</html>
